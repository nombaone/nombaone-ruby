# frozen_string_literal: true

module Nombaone
  # The installed SDK version. Sent on every request as part of the
  # `User-Agent` (`nombaone-ruby/<version>`) and used for release tooling.
  VERSION = "0.1.0"
end
